# Outline — authoritative source of truth

**Strong SDD:** this file, not `slides.md`, is the truth for what the deck says.
`slides.md` *renders* these nodes. Isaiah's authored copy here is verbatim source
text — **do not paraphrase it during regeneration; preserve his voice.**

## How to read this

- Each node has a **stable ID** (`arc-01`, `arc-02`, …). Slides trace to an ID.
- **Purpose** — why the slide exists (which part of the brief's core message it
  serves). If you can't name one, the slide shouldn't exist.
- **Copy** — the authored headline + key lines, in Isaiah's voice. This is the
  voice-bearing layer: treat it as source text, not a suggestion.
- Order here is authoritative. Reorder *here*, then regenerate the slides.

---

### arc-01 · <working title>

- **Purpose:**
- **Copy:**
  - Headline:
  - Key line(s):
- **Traces to:** `slides.md` slide <n>
- **Sources:** `context/<file>.md` (if a claim is made)

<!-- Duplicate the block above per slide. Keep IDs stable even if titles change. -->
